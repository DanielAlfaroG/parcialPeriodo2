import 'package:flutter/material.dart';
import 'package:parcialperiodo2/pagina.dart';

class tortas extends StatelessWidget {
  tortas({Key ?key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      home: Pagina(),
    );
  }
}
